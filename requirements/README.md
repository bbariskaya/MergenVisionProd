# requirements/

This directory is for requirements, constraints, and acceptance-criteria documents.

Requirements must not silently override source-of-truth decisions in `AGENTS.md` or `phase0beforestarting.md` without explicit user approval.

If a requirement conflicts with the model-validation gate, the gate wins.
